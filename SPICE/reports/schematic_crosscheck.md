# KiCad schematic / exported-netlist cross-check

Placed schematic symbols: **416**; exported netlist components: **216**.

Blocking cross-check failures: **0**; metadata warnings: **0**.

Every exported reference is present as a placed schematic symbol and all exported footprints match the placed-symbol footprints after library-prefix normalization.
